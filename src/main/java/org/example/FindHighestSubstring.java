package org.example;

public class FindHighestSubstring {
    public static void highestSubstring (String str) {
        String newStr = str.toLowerCase();
        int count = 0;
        for(int i = 0; i<newStr.length(); i++){
          String subStr = newStr.substring(i, i+ 1);
//          if()
            System.out.println("Substring " + subStr);
//          str.
        }
    }
}
